import {locale} from 'constants/locale/EsEs';

export const fuelTypesObj = {
  1: locale.Petrol,
  2: locale.Diesel,
};

export const concertStatusesObj = {
  0: locale.Draft,
  1: locale.Confirmed,
  draft: 0,
  confirmed: 1
};

export const cookiesObj = {analytics: 'analytics'};

export const clubStatusesObj = {
  0: locale.Unknown,
  5: locale.Opened,
  10: locale.Closed,
  unknown: 0,
  opened: 5,
  closed: 10
};

export const areasObj = {
  centro: locale.Center,
  periferia: locale.Periphery,
  center: 'centro',
  periphery: 'periferia',
};
